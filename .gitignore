# local WSL test deps (installed via pip --target)
.deps/

# python
__pycache__/
*.pyc

# geoip databases (downloaded from DB-IP on first run)
.geoip/

# recorded sessions
orbit-*.jsonl

# tooling artifacts
.playwright-mcp/
.DS_Store

# MSI build output (PyInstaller freeze + signed installer)
dist/
build/_work/
*.msi
